schema = 1

# Temporary repo-local exceptions belong here, not in clippy.toml carveouts.
# Each entry must include lint, path, owner, reason, and expires.
#
# [[debt]]
# lint = "clippy::arithmetic_side_effects"
# path = "crates/flight-axis/src/*.rs"
# owner = "rt-core"
# reason = "Reviewed real-time numeric lane awaiting checked/wrapping policy."
# expires = "2026-07-01"
