schema_version = "1.0"

# Non-Rust programming/configuration surfaces must be covered by structured
# policy entries. Prefer glob entries for owned surfaces and path entries for
# one-off files.
#
# [[allow]]
# glob = ".github/workflows/*.yml"
# kind = "ci_declarative"
# owner = "release/ci"
# reason = "GitHub Actions workflow definitions are platform-required YAML."
# surface = "ci"
# classification = "config"
# covered_by = ["cargo xtask check-file-policy"]
