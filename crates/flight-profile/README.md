# flight-profile

Profile schema, validation, canonicalization, and merge logic.

## Responsibilities

- Defines profile data structures used by axis and service layers.
- Validates profile content and enforces schema constraints.
- Implements deterministic canonicalization, hashing, and merge semantics.

## Status

Internal workspace crate; APIs can evolve with Flight Hub service requirements.
