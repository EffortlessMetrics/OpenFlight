[package]
homepage.workspace = true
name = "flight-open-hardware"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "OpenFlight reference hardware protocol — HID report definitions for the open-design FFB stick"
readme = "README.md"
keywords = ["hid", "ffb", "joystick", "embedded", "no-std"]
categories = ["embedded", "hardware-support", "no-std"]

# This crate is intentionally no_std so embedded firmware can use the same
# report format constants as the host driver.

[dependencies]
# no external deps — bitfield manipulation is done manually

[dev-dependencies]
