[package]
name = "flight-session-fuzz"
version = "0.0.0"
publish = false
edition = "2024"

[workspace]

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
flight-session = { path = ".." }
serde_json = "1"

[[bin]]
name = "fuzz_session_state"
path = "fuzz_targets/fuzz_session_state.rs"
test = false
doc = false
