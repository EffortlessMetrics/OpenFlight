fix(virtual): fresh health snapshots; drain stale frames; honor E-STOP/faults

Changes:
- flight-virtual OFP1 emulator: read_health_status() now drains channel and
  rebuilds fresh snapshot from atomics, always sets READY, mirrors
  EMERGENCY_STOP, and bumps sequence (copy→modify→write-back on packed fields)
- emergency_stop(), inject_fault(), clear_faults() flush stale frames to avoid
  pre-flag snapshots
- Default caps match tests (OFP1_VERSION, max_torque_mnm=20000,
  min_period_us=500); unit assertion updated accordingly
- OFP1 integration tests: packed-field reads switched to copy first (no E0793)
- Negotiation tests set preferred rate ≥ 2000 Hz so effective rate matches device
- Fault tests use on-demand health (no sim-thread race)
- Loopback tests: avg_latency_us never returns 0 on first tick (stable smoke)
- test_usb_frame_simulation asserts computed expected range with small tolerance
  (typed to avoid E0689)

Internal behavior only; no public API changes.

Verification:
- cargo check --workspace
- cargo bench -p flight-ipc --features ipc-bench --no-run
- cargo bench -p flight-ipc --features "ipc-bench,ipc-bench-serde" --no-run
- cargo test -p flight-hid --features ofp1-tests --no-run
- cargo test -p flight-virtual --test ofp1_integration
- cargo test -p flight-virtual --lib
